release-image:
  extends:
    - .rules:code-changes
    - .docker:use-docker-in-docker
  image: docker:git
  stage: build
  before_script:
    - apk add --no-cache jq curl
  script:
    - ./bin/docker ci-login
    - ./bin/docker ci-build-if-necessary
  after_script:
    - ./bin/docker ci-logout
