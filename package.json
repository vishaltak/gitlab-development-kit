{
  "name": "gitlab-development-kit",
  "version": "0.0.1",
  "description": "GitLab Development Kit",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://gitlab.com/gitlab-org/gitlab-development-kit.git"
  },
  "author": "GDK Team <gitlab_rubygems@gitlab.com>",
  "bugs": {
    "url": "https://gitlab.com/gitlab-org/gitlab-development-kit/issues"
  },
  "homepage": "https://gitlab.com/gitlab-org/gitlab-development-kit#readme",
  "scripts": {
    "check-links": "find doc -name \\*.md -print0 | cat - <(echo -n 'README.md') | xargs -0 -n1 markdown-link-check -qq --config markdown-link-check-config.json | awk 'BEGIN {ec=0 ; p=0}; /FILE: / {S=$0 ; p = 1 ; next}; /Status: */ {ec=1 ; p=0 ; print S, \" - \" $0}; END {exit ec}'"
  },
  "dependencies": {
    "markdown-link-check": "^3.9.0",
    "markdownlint-cli": "^0.30.0"
  }
}
