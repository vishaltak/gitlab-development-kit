<?xml version="1.0"?>
<clickhouse>
    <openSSL>
        <server>
            <certificateFile><%= GDK.config.clickhouse.dir.join('server.crt') %></certificateFile>
            <privateKeyFile><%= GDK.config.clickhouse.dir.join('server.key') %></privateKeyFile>
            <dhParamsFile><%= GDK.config.clickhouse.dir.join('dhparam.pem') %></dhParamsFile>
        </server>
    </openSSL>
</clickhouse>
